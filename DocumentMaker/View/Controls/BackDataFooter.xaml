<UserControl
    x:Class="DocumentMaker.View.Controls.BackDataFooter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="7*" />
            <ColumnDefinition x:Name="IsSketchColumn" MaxWidth="90" />
            <ColumnDefinition MaxWidth="90" />
            <ColumnDefinition Width="50" />
        </Grid.ColumnDefinitions>

        <Button
            Grid.ColumnSpan="4"
            Click="AddBtnClick"
            Content="Добавити"
            Style="{StaticResource BackDataAddButton}" />

        <TextBox
            Grid.Column="2"
            Style="{StaticResource BackDataAllTimeTextBox}"
            Text="{Binding AllTime, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <Button
            Grid.Column="3"
            Click="DeleteBtnClick"
            Style="{StaticResource BtnRoundedCross}" />

    </Grid>
</UserControl>
