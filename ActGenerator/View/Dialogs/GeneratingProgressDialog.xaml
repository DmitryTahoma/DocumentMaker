<UserControl
    x:Class="ActGenerator.View.Dialogs.GeneratingProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="CodeBehind"
    MinWidth="400"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Style="{StaticResource GeneratingProgressDialogTextBlock}" Text="{Binding ElementName=CodeBehind, Path=LabelText, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

        <ProgressBar
            Grid.Row="1"
            Maximum="{Binding ElementName=CodeBehind, Path=ProgressMaximum, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
            Style="{StaticResource GeneratingProgressDialogProgressBar}"
            Value="{Binding ElementName=CodeBehind, Path=ProgressValue, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" />

    </Grid>

</UserControl>
